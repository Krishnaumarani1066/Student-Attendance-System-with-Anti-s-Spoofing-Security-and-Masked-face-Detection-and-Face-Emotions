# Web Framework & Database
Flask>=2.3.0
Flask-Compress>=1.14
Werkzeug>=2.3.0
pymongo>=4.5.0

# Computer Vision (Lightweight Headless Versions)
# Avoids loading heavy GUI dependencies to save RAM
opencv-python-headless>=4.8.0
Pillow>=10.2.0

# Face Recognition
# We install torch separately in the build command to control memory
face-recognition>=1.3.0
face-recognition-models>=0.3.0

# Emotion Detection (CPU Optimized)
# Using 'tensorflow-cpu' is much lighter than standard 'tensorflow'
deepface>=0.0.79
tf-keras>=2.15.0
tensorflow-cpu>=2.15.0

# Scientific & Data Processing
# Restricting numpy version to ensure compatibility with dlib/face-recognition
numpy<2.0.0
scipy>=1.11.0
pandas>=2.0.0
matplotlib>=3.8.0

# Utilities & Email
python-dateutil>=2.8.0
flask-mail>=0.9.0
