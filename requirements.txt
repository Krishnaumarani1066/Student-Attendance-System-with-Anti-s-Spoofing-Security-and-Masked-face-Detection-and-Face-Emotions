# Student Attendance System - Requirements
# Install with: pip install -r requirements.txt
# Updated for Python 3.11+ compatibility (Render.com deployment)

# Web Framework
Flask>=2.3.0
Flask-Compress>=1.14
Werkzeug>=2.3.0

# Database
pymongo>=4.5.0

# Computer Vision & Image Processing
# Use headless version to reduce dependencies
opencv-python-headless>=4.8.0
Pillow>=10.2.0
# MediaPipe is optional - install manually if needed: pip install mediapipe
# mediapipe>=0.10.0

# Face Recognition (includes dlib)
# Try to use pre-built wheels to avoid compilation
face-recognition>=1.3.0
face-recognition-models>=0.3.0

# Deep Learning & ML
# Note: Install CPU-only versions manually to save memory:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0
torchvision>=0.20.0

# Emotion Detection
deepface>=0.0.79
# TensorFlow and tf-keras updated for Python 3.13 compatibility
tf-keras>=2.20.0
tensorflow>=2.20.0

# Scientific Computing
numpy>=1.26.0
scipy>=1.11.0

# Data Analysis & Visualization
pandas>=2.0.0
matplotlib>=3.8.0

# Utilities
python-dateutil>=2.8.0

# Email
flask-mail>=0.9.0
